// Página de checkout. Muestra el componente principal de pago.
import { Checkout } from "@/components/checkout";

export default function CheckoutPage() {
  // Renderiza el componente de checkout
  return <Checkout />;
}
